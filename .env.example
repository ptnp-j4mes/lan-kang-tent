# ---- Database ----
DATABASE_URL="postgresql://campthai:campthai@localhost:5432/campthai?schema=public"
REDIS_URL="redis://localhost:6379"

# ---- API ----
API_PORT=4000
API_URL="http://localhost:4000"

# ---- Auth ----
JWT_SECRET="change-me-in-production"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
FACEBOOK_CLIENT_ID=""
FACEBOOK_CLIENT_SECRET=""
# OAuth callback uses API_URL: {API_URL}/api/auth/oauth/{google|facebook}/callback

# ---- Google Maps / Places (server-side key, restricted) ----
GOOGLE_MAPS_SERVER_KEY=""

# ---- GA4 Data API (SEO auto-generate) — service account, read-only analytics ----
# Property ID is set in the admin SEO page (DB). Only the SA creds live here.
GA4_SA_CLIENT_EMAIL=""
GA4_SA_PRIVATE_KEY=""

# ---- Web (public) ----
NEXT_PUBLIC_API_URL="http://localhost:4000"
# Browser key for Maps JS API — restrict by HTTP referrer
NEXT_PUBLIC_GOOGLE_MAPS_BROWSER_KEY=""
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=""

# ---- Backoffice (webmange / Vite) — fully separate admin app, own login ----
VITE_API_URL="http://localhost:4100"

# ---- Storage (R2 / S3) ----
S3_ENDPOINT=""
S3_BUCKET="campthai"
S3_ACCESS_KEY=""
S3_SECRET_KEY=""
